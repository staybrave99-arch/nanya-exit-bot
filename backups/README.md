# 備份

Fly.io 部署移除前，從 volume（`/data`）備份下來的最後一份狀態，純粹保存用，
不會被程式讀取。

- `state-2026-08-10.json`：移除當下的完整持倉追蹤狀態（R1/R2 已成交共 12 張，
  已實現均價 482.5，剩 48 張）
- `price_cache-2026-08-10.csv`：對應的本地價格快取

要接回實盤的話，把 `state-2026-08-10.json` 複製成 `state.json`（或用
`STATE_PATH` 指向它）即可從這個進度繼續。
